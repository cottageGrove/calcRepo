# Starting with Watson Assistant V2, declared services are no longer needed as they are configured
# through the cloud interface. Only the name of the assistant is relevant and matches the 
# name of the assistant targeted by the application.
applications:
- name: CalculatorAssistant
  command: npm start
  path: .
  memory: 256M
  instances: 1
